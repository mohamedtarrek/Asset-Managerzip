kind = "web"
previewPath = "/"
title = "Svarog — Solana Token Launcher"
version = "1.0.0"
id = "artifacts/svarog"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20984

[services.development]
run = "pnpm --filter @workspace/svarog run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/svarog", "run", "build" ]
publicDir = "artifacts/svarog/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20984"
BASE_PATH = "/"
